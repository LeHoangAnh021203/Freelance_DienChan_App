# Local Storage

Client hiện dùng `localStorage` cho cài đặt cá nhân.

## Keys

- `dc_settings`: bố cục, cỡ chữ, thứ tự thẻ, preset và nhắc lịch.

## Migration Notes

Khi có backend, dữ liệu này nên được chuyển thành `user_settings` và đồng bộ sau đăng nhập.
