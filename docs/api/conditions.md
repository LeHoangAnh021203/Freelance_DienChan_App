# Conditions API

## `GET /api/v1/conditions`

Danh sách bệnh và triệu chứng.

## `GET /api/v1/conditions/search?q={keyword}`

Tìm kiếm theo từ khóa người dùng nhập.

## Response Fields

- `id`
- `type`
- `name`
- `group`
- `keywords`
- `protocolIds`
- `hasSafetyRisk`
